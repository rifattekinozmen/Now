APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
# Tam Docker (nginx+app): http://localhost:8080  |  Yol B (host PHP): http://127.0.0.1:8000
APP_URL=http://localhost:8080

APP_LOCALE=tr
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug
# Operasyonel bildirimler (sevkiyat gönderildi vb.) — logging.php kanal adı
# OPERATIONS_LOG_CHANNEL=single
# Slack Incoming Webhook (boş = Slack tarafı atlanır)
# OPERATIONS_SLACK_WEBHOOK_URL=
# OPERATIONS_SLACK_TIMEOUT=5

# =============================================================================
# Veritabanı — Docker Compose (Now için önerilen)
# Artisan’ı host’ta değil, `docker compose exec app php artisan ...` ile çalıştırın.
# =============================================================================
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=now
DB_USERNAME=now
DB_PASSWORD=secret
DB_ROOT_PASSWORD=rootsecret

DOCKER_HTTP_PORT=8080
# 33060 sık dolu olur (başka MySQL/Docker); çakışırsa 33061, 33062… deneyin
DOCKER_MYSQL_PORT=33061
DOCKER_REDIS_PORT=63790

# =============================================================================
# Yol B — PHP + Node bilgisayarda; yalnızca MySQL + Redis Docker (hızlı geliştirme)
#
# 1) composer run docker:infra
#    (veya: docker compose up mysql redis -d)
# 2) .env içinde konteyner host adlarını KAPATIP aşağıdakileri kullanın (üstteki mysql/redis satırlarını yorum satırı yapın veya silin):
#    APP_URL=http://127.0.0.1:8000
#    DB_HOST=127.0.0.1
#    DB_PORT=33061
#    REDIS_HOST=127.0.0.1
#    REDIS_PORT=63790
# 3) php artisan config:clear
# 4) composer run dev  →  http://127.0.0.1:8000
#
# Host’ta phpredis yoksa (çoğu Windows kurulumu): REDIS_CLIENT=predis  (predis paketi projede)
# Durdurmak: composer run docker:infra:down
# =============================================================================
# DB_HOST=127.0.0.1
# DB_PORT=33061
# REDIS_HOST=127.0.0.1
# REDIS_PORT=63790
# REDIS_CLIENT=predis

# =============================================================================
# Alternatif: SQLite (Pest / CI / Composer setup, MySQL olmadan)
# =============================================================================
# Yerel test: `php artisan config:cache` kullandıysan önce `php artisan config:clear` çalıştır
# (veya TestCase otomatik siler); aksi halde önbellek Docker mysql host'unu zorlar.
# DB_CONNECTION=sqlite

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
# Üretimde zamanlanmış görevler (örn. `logistics:refresh-tcmb-rates` günlük 09:10): crontab veya supervisor ile `* * * * * php artisan schedule:run`

CACHE_STORE=redis
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# TotalEnergies (yakıt fiyatı — şimdilik stub; gerçek API için anahtar ve sözleşme gerekir)
TOTALENERGIES_ENABLED=false
# TOTALENERGIES_API_KEY=
# TOTALENERGIES_BASE_URL=https://api.totalenergies.example
